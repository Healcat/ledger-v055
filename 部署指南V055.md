# 记账本 V055（手机版 PWA）· 免费部署指南

## 一、V055 是什么
V055 是把 V050 桌面版**改造为手机 App 形态**的版本，技术上是一个 **PWA（渐进式 Web 应用）**：

- 界面已做移动端适配（安全区、44px 触摸区、iOS 防缩放、各断点响应式、iOS 文件选择 overlay）；
- 可**安装到手机主屏**，图标与原生 App 一致，全屏运行、无浏览器地址栏；
- 配 **Service Worker**，首次联网加载后**断网也能离线打开**；
- 用**独立 IndexedDB 库名 `ledger-cache-v055`**，**不会与 V025/V050 互相冲突**（这正是 V025 曾报错"本地缓存不可用"的根因）。

## 二、为什么是 PWA（而不是原生 App）？
"手机 App + 免费部署"只有 PWA 能同时满足：

| 方案 | 免费？ | 手机安装 | 说明 |
|---|---|---|---|
| **PWA（本方案）** | ✅ 完全免费 | ✅ 主屏图标（Android/iOS 均支持） | 静态托管即可，GitHub Pages / Cloudflare / Vercel / Netlify / Gitee 都免费 |
| 原生 Android（Capacitor） | 构建免费，但**分发**需 Google Play $25 一次性，或侧载 APK | ✅ | 上架商店付费；侧载需开"未知来源"，iOS 不支持 |
| 原生 iOS（Swift） | ❌ **$99/年** 开发者订阅 | ✅ | 不上架根本装不上，不符合"免费" |

结论：**免费把网页变成"手机 App 体验"，PWA 是唯一答案。**

## 三、免费托管平台对比

| 平台 | 免费额度 | 国内访问 | 自动 HTTPS | 部署方式 |
|---|---|---|---|---|
| **GitHub Pages** | 无限静态页（公开仓库） | 一般（有时慢） | ✅ | `deploy_github.sh` 一键 |
| **Gitee Pages** | 免费（需实名） | ✅ 快 | ✅ | `deploy_gitee.sh` + 网页点一次开 Pages |
| **Cloudflare Pages** | 无限请求/带宽 | ✅ 快（全球 CDN） | ✅ | 连 Git 仓库自动构建，或 `wrangler` 直传 |
| **Vercel** | 100GB/月 带宽 | ✅ | ✅ | 连 Git 仓库，push 即部署 |
| **Netlify** | 100GB/月 带宽 | ✅ | ✅ | 拖文件夹 / 连 Git |

> 全部**无需服务器、无后端**，因为 V055 是纯前端（数据存浏览器 IndexedDB）。

## 四、部署步骤

### 方式一：GitHub Pages（已备脚本，推荐）
```bash
cd 记账本V055
bash deploy_github.sh
```
脚本自动：从本机 git 凭据取 Token → 建 `ledger-v055` 公开仓库 → 推送单文件版 → 开 Pages。
完成后访问：`https://<你的用户名>.github.io/ledger-v055/`

### 方式二：Gitee Pages（国内快，需手动开 Pages 一次）
```bash
cd 记账本V055
GITEE_TOKEN=你的私人令牌 bash deploy_gitee.sh
```
推送后，到 Gitee 仓库「服务 → Gitee Pages」点「启动」（API 不支持自动开）。
访问：`https://<用户名>.gitee.io/ledger-v055/`

### 方式三：Cloudflare Pages（全球 CDN，最快）
1. 把 `记账本V055` 目录推到一个 Git 仓库（或直接用 wrangler）；
2. Cloudflare 控制台「Pages → 连接 Git → 选仓库」；
3. 构建命令留空（已是静态），输出目录填 `.`（或直接上传单文件 `记账本_fitWMV055.html` 重命名为 `index.html` + `sw.js`/`manifest.webmanifest`/三个图标）。

### 方式四：Vercel / Netlify
- Vercel：导入 Git 仓库，Framework 选 `Other`，Output 目录 `.`；
- Netlify：把目录拖到 app.netlify.com，或连 Git。

> 无论哪种，托管根目录需包含：`index.html`（=单文件版 `记账本_fitWMV055.html` 改名）、`sw.js`、`manifest.webmanifest`、`icon-192.png`、`icon-512.png`、`icon-maskable-512.png`。

## 五、在手机上"安装"成 App

### Android（Chrome / Edge）
打开站点 → 地址栏右侧出现 **「安装应用 / 安装」** 图标（或菜单 → 安装应用）→ 确认。
之后主屏出现"记账本"图标，点开即全屏运行，可离线。

### iPhone / iPad（Safari）
Safari 打开站点 → 点底部 **分享** 按钮 → **「添加到主屏幕」** → 主屏出现图标。
（iOS 不支持自动安装弹窗，故 V055 在 Safari 会显示一行引导提示。）

## 六、离线 & 更新
- **离线**：Service Worker 首次加载后缓存全部资源，断网也能开（IndexedDB 本地数据不依赖网络）。
- **更新**：导航请求**网络优先**，部署新版本后刷新即取最新；`sw.js` 缓存版本号为 `ledger-v055-v1`，改版时升版本即可强制刷新客户端缓存。

## 七、注意事项
1. **必须用 HTTPS 访问**（含 localhost）才能安装 PWA；`file://` 直接双击打开**无法安装**（但功能可用，仅不能"加到主屏"）。
2. 各版本库名隔离：V055=`ledger-cache-v055`、V050/V025=`ledger-cache`，互不影响，可并存。
3. 图标已生成（深色圆角底 + 品牌色柱状图），满足 Chrome 安装所需的 192/512/maskable 尺寸。
4. 单文件版 `记账本_fitWMV055.html` 可单独发给别人离线用；在线部署请用 `index.html`（已内联全部依赖）。
